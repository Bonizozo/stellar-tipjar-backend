# Cargo configuration for the Stellar Tipjar backend.
#
# This file ensures the project builds correctly in Git Bash / MSYS2
# environments where /usr/bin/link shadows the MSVC linker.
# We target x86_64-pc-windows-gnu which uses gcc/ld instead of link.exe.

[build]
target = "x86_64-pc-windows-gnu"
